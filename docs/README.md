# 项目文档

本目录只保留设计、验收和资源契约等文本资料。原有 445 张设计与 QA 截图已迁移到喜马 CDN，
避免二进制审计产物持续放大 Git 仓库。

- 文档中已引用的截图使用 HTTPS CDN 链接直接展示。
- 未被正文引用的历史截图可通过 [`REMOTE_IMAGE_MANIFEST.json`](./REMOTE_IMAGE_MANIFEST.json) 按原相对路径查找。
- CDN 当前使用测试域名；迁移正式环境时只需批量替换清单及 Markdown 中的域名。
